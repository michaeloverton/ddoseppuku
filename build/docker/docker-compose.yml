version: '3'

services:

  sentinel:
    image: michaeloverton/sentinel
    env_file:
      - ../../.env
    ports:
       - 3000:3000
    restart: always

  target:
    image: michaeloverton/target
    env_file:
      - ../../.env
    ports:
       - 8080:8080
    restart: always
